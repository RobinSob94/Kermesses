name: kermesses
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.1
  image_picker: ^0.8.7+3
  intl: ^0.17.0
  flutter_secure_storage: ^9.2.2
  flutter_dotenv: ^5.1.0
  jwt_decode: 0.3.1
  flutter_stripe: ^10.2.0
  firebase_core: ^3.2.0
  firebase_messaging: ^15.0.3
  shared_preferences: ^2.0.8

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - .env  # Ajoute le fichier .env ici
